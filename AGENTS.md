# TradingAgents A 股选股工作台

## 项目目标

这是一个本地运行的 A 股动态选股工作台。它以公开行情为数据源，提供按市场范围筛选、因子排序、板块强度、沪深主板涨停打板候选、股票详情、相关概念、日 K 线和指定交易日分时图。

页面面向日常看盘与初筛：信息应一眼可读，行情数据与策略结论必须分开表达；它不是投资建议，也不应将缓存行情描述为实时无延迟数据。

## 文件与架构

- `market_server.py`：仅使用 Python 标准库的 HTTP 服务、公开行情适配、筛选因子、缓存和 JSON API。
- `trading-agents-stock.html`：单文件前端，包含样式、页面结构和原生 JavaScript 状态管理与 SVG 图表。

服务直接读取同目录的 HTML 文件，因此修改前端后刷新浏览器即可生效，不需要前端构建步骤。

## 本地运行与验证

启动服务：

```bash
python3 market_server.py --host 127.0.0.1 --port 8502
```

打开 `http://127.0.0.1:8502/`。端口被占用时使用新的本地端口，不要终止用户已启动的服务。

改动后按影响范围验证：

```bash
python3 -m py_compile market_server.py
node -e 'const fs=require("fs"); const h=fs.readFileSync("trading-agents-stock.html","utf8"); new Function(h.split("<script>")[1].split("</script>")[0]); console.log("HTML script OK")'
curl -fsS http://127.0.0.1:8502/api/health
```

涉及接口时，补充验证对应的本地 API；涉及布局、选择状态或图表交互时，用浏览器在桌面和窄屏下实际检查。

## HTTP API

所有 API 均为 `GET`，JSON 响应使用 `Cache-Control: no-store`：

- `GET /api/health`：服务健康状态。
- `GET /api/sectors`：固定板块导航数据。
- `GET /api/quotes?sector=<key>&refresh=1`：固定板块样本行情。
- `GET /api/screener?market=<key>&minAmount=<number>&minChange=<number>&minTurnover=<number>&limit=<20-150>&refresh=1`：动态选股结果。
- `GET /api/sector-strength?market=<key>&refresh=1`：当日行业强度与各行业核心强势票快照。
- `GET /api/hot-concept?name=<概念名称>&refresh=1`：解析东方财富概念板块并返回其成分股实时行情。
- `GET /api/hot-concept-summary?names=<逗号分隔概念>&refresh=1`：仅返回指定一组概念的实时平均涨跌与成分数量，用于左侧热门板块的按需配色；单次最多 8 个概念。
- 热门概念名称解析依次使用规范化精确匹配、项目维护的常用别名和高阈值相似名称匹配；接口返回 `resolvedConcept` 与 `matchType` 便于核验。不得因搜索结果非空就直接采用第一项，低于可靠阈值时必须明确报错，不能把无关板块伪装成目标概念。同花顺、腾讯若未来提供稳定可核验的公开概念成分接口，可作为独立后备源接入。
- `GET /api/limit-up?refresh=1`：沪深主板涨停打板候选，包含冲板评分、理论涨停价、过去一年触板/封板/炸板统计，以及已接入时的 09:25 竞价委买因子。
- `GET /api/stock?code=<6位代码>&refresh=1`：单股行情、K 线和基础信息。
- `GET /api/concepts?code=<6位代码>`：热门相关概念。
- `GET /api/intraday?code=<6位代码>&date=YYYY-MM-DD&refresh=1`：指定交易日分时数据。

`market` 的展示和顺序必须保持为：`all`（全部类型）、`sh-main`（主板上海）、`sz-main`（主板深圳）、`chinext`（创业板）、`star`（科创板）、`bse`（北交所）。新增或变更范围时，要同时调整服务端 `MARKET_SCOPES` 与前端的可见标签。

## 行情与选股规则

- 公开数据源包括腾讯证券、东方财富延迟行情、东方财富热门概念、同花顺公开个股人气与新浪证券不复权日线。同花顺人气仅为最终候选池补充，缓存 10 分钟以降低公开站点压力；请求失败时保留明确错误状态，不能伪造行情或静默回退为过期样例。
- 缓存用于降低公开接口压力。修改 TTL、并发数、请求超时或数据源前，应说明对延迟、稳定性和数据一致性的影响。
- 动态评分目前只基于真实行情因子。基本面、新闻、TradingAgents/LLM 研判尚未接入时，不得把它们写成已生效评分来源。
- 动态选股必须排除“近期阴跌”股票。现有判定由近 30 日日 K 线确认，包含均线空头结构、阶段累计跌幅、下跌日占比等条件；修改时保持它是硬过滤而非仅降低评分，并同步更新入选理由和计数。
- 市场切换动态候选池中，主板、创业板与科创板必须排除过去 365 日内没有“收盘封板”记录的股票；历史日线不可确认时同样不进入候选池。北交所不应用这条涨停历史过滤。该硬过滤复用涨停历史日线缓存，首次刷新可能较慢。
- 市场切换动态候选池中，除北交所外，总市值必须不低于 30 亿元；总市值缺失或低于门槛的股票不进入候选池。北交所代码不应用此市值门槛。
- 涨停打板页只扫描上海主板 `600/601/603/605` 与深圳主板 `000/001/002/003`，排除 ST、退市标识、停牌和无有效价格的股票。基础候选要求当日涨幅不低于 2%、成交额不低于 1 亿元、流通市值严格大于 30 亿元；过去 365 日封板次数仅用于展示与评分，不作为入选门槛。
- 打板评分为 100 分制：板块共振 20%、攻击强度 20%、成交活跃 12%、换手结构 8%、过去一年封板历史 8%、流通盘适配 8%、价格稳定性 4%、09:25 竞价委买强度 20%；触板后开板的候选额外扣 12 分。竞价委买强度基于 09:25 撮合后五档未匹配委买额，并结合流通市值和竞价成交额归一化；未接入时使用明确标注的中性 50 分，绝不把缺失数据视作零委买。评分只用于短线观察排序，不构成涨停预测或投资建议。
- 涨停候选卡必须逐项展示条件是否命中：板块前列、5%–9.8%强攻、板块联动、有效成交额、量比放大、换手适中、流通市值>30亿、一年封板记录、20–60 个交易日涨停记忆、开盘承接稳定。绿色“✓”表示当前行情满足，灰色“○”表示未满足；基础门槛为市场范围、非 ST/退市、有效行情、涨幅、成交额和流通市值，其他展示条件不是单独硬过滤。
- 历史涨停圆圈使用过去 365 日“收盘封板次数”，所有候选均显示真实数字（包括 0、1、2）。理论涨停价按前收盘价乘以 1.10 并四舍五入至 0.01 元计算；同时保留最高价触板次数和炸板次数。日线缓存扩展为约 750 个交易日，以支持近 3 年后续上涨模型回测；涨停统计仍严格按最近 365 个自然日计算。历史统计使用不复权日线，缓存 12 小时，避免盘中重复拉取。
- 涨停候选卡右上角除历史次数圆圈外，可显示当日板型方框：`一字`（开高低收均封在涨停价）、`回封`（日内低于涨停价、收盘重新封板）或`炸板`（盘中触及涨停价、当前未封板）。三者均不满足时不展示方框；颜色分别为红、青、橙。
- 所有数值解析都要容忍 `None`、缺失字段与非交易时段。前端显示缺失数据时使用明确占位，禁止把缺失值当作 0 或涨跌平。

## 前端交互约束

- 默认深色模式，支持日间模式；主题通过 `tradingagents-stock-selector-theme` 保存。两种模式都要保持红涨绿跌。
- 左侧边栏不提供独立的“全部类型”入口，“市场切换”菜单统一显示为“盘中选股”；全市场范围仍可通过页面内的选股范围下拉框选择。
- 左侧“盘中选股”上方提供独立一级菜单“中期选股”，扫描全部 A 股市场（主板、创业板、科创板与北交所），排除 ST/退市标识。当前候选保存在本机 `data/backtest.sqlite`：每天加入满足条件的股票；形态走坏或不再满足条件时直接移出当前池，后续再次满足时重新进入。进入次数由交易日去重记录累计。
- 中期入池规则中，主板与北交所沿用低位反弹和均线条件；创业板、科创板只判断当年1月以来7月是否创出阶段新低，以及8月是否开始反弹。成长板块不再要求此前1/2年低点或均线条件。主板与北交所仍要求 MA5、MA10、MA20、MA30 至少3条向上。
- 中期选股首次全市场扫描在后台分批执行，页面每5秒读取进度，不阻塞已有候选展示。卡片采用热门板块的信息图风格，展示首次入池日期、进入次数、7月低点、此前1年/2年低点、8月反弹幅度和均线方向。
- 中期池页面对当前候选再应用做 T 硬筛选：当前成交额不低于5亿元，7月最低点到最新行情价涨幅严格大于10%且小于50%；创业板与科创板涨幅区间调整为严格大于20%且小于50%；同时8月以来至少20%交易日日振幅不低于4%。换手率暂不参与做 T 展示筛选。未通过的股票不进入当前展示；形态重新满足后可再次入池。这些硬条件只用于后台筛选，不得在卡片、标签或状态栏中逐项展示；卡片保持中期形态风格，展示进入次数、8月反弹、长期低点和均线方向。
- 中期选股历史日线写入本机 `data/backtest.sqlite` 的 `midterm_history_cache`：首次扫描获取约750个交易日，后续只请求最近45个交易日并按日期合并，避免重复下载完整历史 K 线。
- 中期池卡片不得显示“持续观察”；个股名称右侧显示最新价和当日涨幅，并换行展示。卡片第一个信息块展示“进入次数”，按行情交易日去重累计：某只股票当日满足做 T 展示筛选时记录1次，同一交易日多次刷新不重复累加。
- 候选卡片单击只选择并保持高亮，不得触发重新排序、完整列表重绘或位置跳动。双击才打开日 K 线弹窗。
- 排序列表前两项依次为“今日涨停概率”“后续上涨概率”，默认使用“今日涨停概率”；两者均按概率从高到低排列，缺失概率排在最后。“股票价格”必须位于“成交额”之前，按现价从高到低排列，缺失价格排在最后。
- 动态选股最低综合分是 60 分硬下限：滑块默认值与最小值均为 60，低于 60 分的股票不得进入候选。拖动分数滑块时即时更新候选，不显示或依赖额外“筛选”按钮；成交额、涨跌幅和换手率等服务端筛选条件在用户调整完成后自动重新加载。
- 市场切换不提供绕过最低分的开关。最低综合分滑块默认值和最小值均为 60，拖动时在标签旁实时显示当前分数。结果使用分页展示，默认每页 100 只，可选 20、50、100、200 只，并提供首页、上一页、下一页、末页。
- 市场切换候选卡不重复显示“市场动态扫描”或市场范围标签；卡片使用双列行情指标展示总市值/流通市值、昨收价格/理论涨停价、竞价涨幅/最低涨幅与量比/换手率。竞价涨幅按开盘价相对昨收计算，最低涨幅按日内最低价相对昨收计算；理论涨停价按主板 10%、创业板/科创板 20%、北交所 30% 计算并四舍五入至 0.01 元，仅作展示参考。
- 市场切换候选卡同时展示量比/换手率，并在个股名称下展示 5、10、20、30 日线价格方框。均线低于现价显示红色框，均线高于现价显示绿色框，相等或数据不足显示灰色框；均线使用动态选股已有的近 30 日日线计算。
- 市场切换候选卡展示“今日涨停概率”和“后续上涨概率”，两者均为 1–95% 的模型参考，严禁描述为保证或投资建议。今日涨停概率综合当日攻击、量比、换手、主力净流入、板块强度、历史封板和已接入时的竞价委买；后续上涨概率以“未来 10 个交易日内最高价较信号日收盘上涨至少 5%”为唯一回测目标，先用近 30 日日 K、筑底/均线/布林结构、碎步小阳、红三兵、一阳包三阴、圆弧底与风险形成原始分档，再按本地历史样本分档校准。数据缺失时相应因子降权或显示缺失，不得伪造。
- 市场切换候选卡把两项概率合并为“今日涨停概率 / 后续上涨概率”，并增加“内盘 / 外盘”。内外盘来自腾讯实时批量行情，委比由五档买卖挂单量计算；缺失时显示 `--` 且不参与评分，严禁按 0 处理。
- “内盘 / 外盘”标签与数值必须分色显示：内盘使用深绿色，外盘使用深红色；具体数字加粗并加深对应颜色，保持 A 股红涨绿跌语义。
- 内外盘属于容易受盘口行为影响的低可信因子，综合评分中最多以 8% 的低权重作为确认项。盘口确认分必须同时结合委比、当日价格位置、量比、K线形态、MA5/10/20/30 位置和行业板块强度；内外盘方向与委比明显背离时降低得分。不得仅因外盘大于内盘就给出高分或买入结论。
- 今日涨停概率同样参考盘口确认分，但只允许小幅修正：以盘口确认分 50 为中性，每偏离 1 分修正 0.08 个百分点，理论影响约为 -4 至 +4 个百分点。盘口缺失时修正为 0；悬浮说明必须显示盘口确认分和实际修正值。
- 后续上涨模型的日线回测样本保存在本机 `data/backtest.sqlite`（已被 Git 忽略）：服务运行在交易日 15:05–15:30 时自动保存一次当日候选快照；某个快照拥有完整后续 10 个交易日后，自动结算并写入校准样本。历史日线只能回测 K 线可还原的因子，不能倒推当时的实时竞价、盘口、资金流或人气；页面悬浮提示必须展示当前样本量和固定目标。服务未在收盘时运行不会补造当日实时快照。
- 服务启动后会在后台对全市场股票分批建立 `all-market` 日线回测样本，已完成股票自动跳过并支持断点续跑；进度可通过 `/api/health` 的 `allMarketBacktest` 查看。全市场批处理只回测历史 K 线可还原的因子，不伪造历史盘口、竞价、资金流或人气。
- 概率视觉高亮阈值：今日涨停概率大于或等于 90% 时用红色加粗强调；后续上涨概率大于或等于 50% 时也用红色加粗强调。概率数字支持鼠标悬浮与键盘焦点提示，展示对应计算因子、形态与模型参考说明。高亮只表示当前启发式模型分档，不表示确定性或推荐。
- 市场切换候选卡在名称右侧显示同花顺人气徽章：前 10 名红色、11–50 名橙色、其余紫色；人气来源不可用时显示灰色“人气 --”。人气只用于展示，不参与现有选股评分或硬过滤。
- 市场切换候选卡在人气徽章右侧显示一级 / 二级题材与最多 4 个额外关联概念方框，不显示“题材板块”或“关联”文字标签。一级 / 二级按“一级行业 / 一级关联概念”展示，例如“传媒 / 广告营销”；一级行业使用行情行业分类，一级关联概念与额外关联概念使用东方财富热门概念按热度排序后的前 5 项。一级或二级概念数据不可用时仍显示对应位置的“--”，不影响评分或候选资格。
- 市场切换候选卡在板块标签右侧用粉红色方框展示已识别的 K 线形态，包括锤子线、启明星、看涨吞没、双底、圆弧底、金针探底、红三兵、上升三部曲、多方炮、旗形上涨和上升三角形；未满足的形态不展示。形态使用最近约 30 日开高低收进行规则识别，悬浮提示说明判定含义；历史接口缺少可靠成交量时，不得宣称红三兵等形态已经过量能确认。
- 日 K 线弹窗默认显示 120 日，并提供 30、60、120、180、360、全部范围。支持滚轮缩放、拖拽查看更早历史，以及 MA10、MA20、MA30。
- 点击某个日 K 蜡烛切换到该交易日分时图；在分时图内点击返回、关闭按钮、遮罩或 Esc，应回到该股票的日 K 线，而不是直接关闭整个弹窗。
- 股票详情中，“股票简况”和“相关概念”同级。概念按热度排序，前 2 个均显示为“走势关联”，其余显示为普通概念标签。
- 彩色因子进度条必须有可访问的名称或悬浮提示，说明对应的指标含义。
- 板块强度页在工作日 9:15–11:30、13:00–15:00 自动刷新；前台间隔 25 秒，页面位于后台时降至 90 秒，休市阶段停止请求并明确显示状态。
- 左侧“板块强度”下方提供一级菜单“热门板块”，按科技、AI、有色金属、消费、旅游、医药、粮食、军工、金融科技、养殖、能源分组展示概念子菜单；只展示能够可靠匹配且实际返回成分股的概念，当前不展示 AI视频、AI应用、商业百货、化肥、农机、兵装重组、啤酒和乳业。消费分组以“大消费”替换“预制菜”并放在“白酒”之前，页面显示“大消费”，后台映射东方财富“消费风格”板块。菜单使用“液冷”“存储芯片”“稀土永磁”“鸡肉概念”“水产养殖”等可见名称，数据解析时保留常用旧名作为别名，“海工装备”使用规范名称。东方财富搜索建议若把“存储芯片”优先返回为指数 `980138`，必须使用已核验的概念成分板块 `BK1137`，不能降级替换为宽泛的“半导体”。已有分组的新概念必须合并进原分组，不得生成重复二级菜单。点击概念后进入独立成分股页面，卡片沿用市场切换的行情、概率、题材、形态与评分样式。
- “热门板块”总菜单支持展开和收起；其内部科技、消费、AI 等分类不再单独折叠，展开总菜单后直接显示全部分类与概念标签。总菜单展开后按分类依次请求概念强度，避免同时发起全部请求；前台约 30 秒刷新一轮、页面后台约 90 秒刷新一轮，收起后停止轮询。结果返回后立即按平均涨跌配色，负值使用绿色系，非负值使用红色系，选中状态加深同一色系。
- 热门概念页使用独立浅色信息图视觉：蓝色标题、红色核心概率、紫色后续上涨概率、绿色辅助指标、圆形名次和图标化信息分区；不得直接复制参考图的人物、品牌或文字内容。个股卡保留综合评分、今日涨停概率、后续上涨概率、成交额、量比/换手、市值、题材和形态。“后续上涨概率”不得使用绿色，避免与下跌状态色混淆。
- 热门概念成分股与市场切换共用同一套腾讯内盘/外盘盘口确认逻辑：综合评分最多按 8% 低权重计入盘口确认分，今日涨停概率按约 -4 至 +4 个百分点小幅修正。计算顺序必须先补充内外盘、委比及其与位置、量能、K线、均线、板块联动的交叉确认，再计算两项分值；盘口缺失时两处都不调整。
- 热门概念页顶部统计依次展示成分股数量、上涨数量、下跌数量、板块平均涨跌和平均涨停概率；下跌数量使用绿色系标签，并随当前成分股行情实时重新计算。
- 热门概念页最低综合分从 0 开始，进入概念时默认回到 0；返回普通市场切换后恢复最低 60 分。概念当日强度按全部有效成分股涨跌幅等权平均，平均涨跌为负时概念主框和“板块平均涨跌”标签使用浅绿色。左侧已加载过的下跌概念标签保持浅绿色，选中时使用更深绿色；非下跌概念使用红色系，选中时同样加深对应色系。
- 动态选股页在相同交易时段自动刷新；前台间隔 60 秒，页面位于后台时降至 180 秒。筛选扫描比板块聚合更重，不应缩短为高频轮询。
- 涨停打板页沿用交易时段判断，前台每 25 秒、后台每 90 秒刷新；收盘后停止自动请求，手动“刷新候选”保持可用。首次加载需为候选补充一年日线，可能慢于普通行情刷新。
- 如需接入东方财富 EMT 竞价数据，通过本机环境变量 `EMT_AUCTION_COMMAND` 配置一个导出程序，命令中的 `{date}` 会替换为交易日。该程序必须输出 EMT `get_open_call_auction` 的 JSON 数组（或含 `data` 数组的对象），字段至少包含 `symbol`、`open_amount`、`open_volume`、`current_price`、`bid_p1`～`bid_p5`、`bid_v1`～`bid_v5`。服务在交易日 09:25–09:30 自动采集，并将仅含行情派生字段的快照写入 `data/auction/YYYY-MM-DD.json`；该目录已被 Git 忽略，严禁保存 Token、账户或 Cookie。

## 改动原则

- 优先保持单文件、无第三方依赖的现有结构；确有必要引入依赖、持久化存储或 LLM 调用时，先说明原因、成本和隐私影响。
- 后端新增字段时，在前端为加载中、为空和接口失败分别提供状态；前端新增筛选条件时，在 `/api/screener` 服务端同步校验和生效，不能只做视觉筛选。
- 不要把 API Key、Cookie、账户信息或其他凭据写入代码、HTML、文档或提交记录。通过本机 `.env` 或进程环境传入，并确保其被忽略。
- 不要删除或覆盖用户已有改动；当前仓库可能存在未跟踪文件，提交前必须先确认范围。
